# Nginx config para frontend + proxy al backend
# Variables sustituidas por docker-entrypoint.sh al iniciar el container:
#   NGINX_PORT   → puerto en el que escucha (default 80; Railway inyecta PORT)
#   BACKEND_HOST → host:port del backend    (default localhost:8080)

upstream backend {
  server BACKEND_HOST_PLACEHOLDER;
}

server {
  listen NGINX_PORT_PLACEHOLDER;
  server_name _;

  # Frontend (SPA)
  location / {
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri $uri/ /index.html;
    add_header Cache-Control "public, max-age=0, must-revalidate";
  }

  # Assets estáticos versionados (cache agresivo)
  location ~ ^/(assets|dist)/ {
    root /usr/share/nginx/html;
    add_header Cache-Control "public, max-age=31536000, immutable";
    expires 1y;
  }

  # Proxy al backend (API)
  location /api/ {
    proxy_pass http://backend;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;
    proxy_buffering on;
    proxy_buffer_size 4k;
    proxy_buffers 24 4k;
  }

  # Proxy webhooks Meta (SSE / streaming)
  location /webhooks/ {
    proxy_pass http://backend;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_buffering off;
    proxy_cache off;
    proxy_set_header Connection '';
    proxy_connect_timeout 60s;
    proxy_send_timeout 3600s;
    proxy_read_timeout 3600s;
  }

  # Health check directo de nginx (Railway lo usa para saber si el container está vivo)
  # No depende del backend - responde 200 siempre que nginx esté corriendo
  location = /ping {
    access_log off;
    return 200 "ok";
    add_header Content-Type text/plain;
  }

  # Health check del gateway (proxea al backend - solo usar cuando el backend esté disponible)
  location /health {
    proxy_pass http://backend;
  }

  gzip on;
  gzip_types text/css application/javascript application/json;
  gzip_min_length 1000;
}
